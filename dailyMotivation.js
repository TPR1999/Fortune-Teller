const motivationFactory = (string1, string2) => {
    return string1 + string2;
}
const string2 = () => {
    const phrases = ['are gonna kill it!', 'are capable!', 'have to keep going!', 'will never give up!']
    return phrases[Math.floor(math.random() * 4)];
}
string1 = 'You';

console.log(string2);